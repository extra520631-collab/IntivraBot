# ── Core API ───────────────────────────────────────────
# Version floors (not hard pins) so installs succeed across Python 3.10–3.14.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
python-multipart>=0.0.9

# ── Resume text extraction ─────────────────────────────
pdfplumber>=0.11
python-docx>=1.1

# ── Gemini (AI interviews, Phase 4) — called over REST ─
requests>=2.31

# ── Face detection / match + emotion (Phase 5) ─────────
# CPU-only ONNX stack (no TensorFlow — works on Python 3.14).
# Models (YuNet, SFace, FER+) live in models/weights/ and are loaded lazily.
opencv-python-headless>=4.9
onnxruntime>=1.17
numpy>=1.26

# ── Voice biometrics: speaker verify + multi-voice (Phase 6) ─
# Resemblyzer pulls torch (CPU), librosa, scikit-learn, webrtcvad.
# webrtcvad still imports pkg_resources, which setuptools>=81 removed —
# so setuptools MUST stay <81 or the import fails on Python 3.14.
resemblyzer>=0.1.4
setuptools<81

# NOTE: Phase 3 ATS uses a skills-taxonomy + regex engine (fast, no model
# download). spaCy NER can be layered in later without changing the API.
